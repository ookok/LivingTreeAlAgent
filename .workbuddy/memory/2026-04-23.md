# 2026-04-23 Daily Log

## 18:38 - 统一缓存三级压缩 + 模型配置
- **超长 query 截断改进**：QueryNormalizer 粗暴截断（`[:200]`）替换为三级压缩策略
- **QueryCompressor**（新增）：300-500字调用 qwen2.5:1.5b LLM 语义压缩（保留意图+实体，0.5s）；>500字 QueryChunker 分块
- **QueryChunker**（新增）：超长 query 按语义句子/轮次分块，支持对话模式（User/Assistant 标记）
- **关键踩坑**：
  - qwen3.5:4b / qwen3.5:2b 全是思考模型，response=""，答案在 thinking 字段
  - QueryCompressor._check_llm 使用 self._COMPRESS_MODELS（不是 COMPRESS_MODELS）
  - tokenize() 内 `for char in chunk` 在 chunk 未定义时报 UnboundLocalError（chinese 列表为空时）
- **LLM 压缩实测**：317字 → 58字，qwen2.5:1.5b，0.50s
- **模型配置记住**：L0=SmolLM2.gguf(fallback qwen3.5:2b)，L3=qwen3.5:4b，L4=qwen3.6:latest，本地 Ollama

## 17:09 - 项目初始化配置测试
- 通过 Agent 执行深度搜索任务："五一南京有哪些地方好玩"
- 测试成功，深度搜索功能正常运行
- 返回结果包含：必去景点（历史文化/自然风光）、美食推荐、5天4晚行程建议、实用提示（交通/住宿/注意事项/预算）

## 17:17 - 完整系统初始化 + L0-L4 + Agent + 深度搜索全链路分析
- 分析完成 Phase0(AppConfig) → Phase1(L0-L4初始化) → Phase2(HermesAgent) → Phase3(路由决策) → Phase4(深度搜索) 全流程
- 关键路径：L0 SmolLM2意图分类 → SEARCH路由 → IntelligentRouter → L4RelayExecutor → DeepSearchWikiSystem
- 搜索执行：2路并行 × 8条结果 = 16条，CredibilityEvaluator排序，结果write_back到L1/L2/L3缓存

## 17:46 - 深度搜索全链路测试代码编写 & 验证通过
- 编写 test_deep_search_chain.py，测试问题："南京溧水养猪场环评报告"
- 实际测试结果：
  - Phase 0：SystemConfig 加载，models/SmolLM2.gguf 找到 1 个 GGUF
  - Phase 1 L0：SmolLM2 via Ollama (smollm2-test:latest) 就绪，路由耗时 629ms
  - Phase 2 L3：qwen3.5:2b 就绪
  - Phase 3 L4：qwen3.6:latest 就绪（思考模型）
  - Phase 4：L0 SmolLM2 分类 route=search intent=search_query confidence=0.90
  - Phase 5：DeepSearchWikiSystem 生成 5 条来源，平均可信度 60
  - Phase 6：L4 qwen3.6:latest 思考模式（content 为空，从 thinking 字段提取 848 字回答），耗时 48s
- 关键踩坑：qwen3.6:latest 是思考模型，非流式响应 content 字段为空，真实回答在 message.thinking 字段
- 修复方案：1) 优先流式调用 stream=True; 2) 非流式时从 thinking 字段提取最终段落; 3) 加 /no_think 指令尝试关闭思考模式
- 全链路总耗时 51s（L4 推理占主要时间）

## 18:09 - 深度搜索缓存命中率测试
- 编写 test_deep_search_cache.py，加入三层缓存（L0/Search/L4）
- 测试问题：五一杭州有哪些玩的，6轮对比（无缓存×3 + 有缓存×3）

关键发现：
- Round 1：无缓存，首次推理 24.3s（L0 620ms + L4 23691ms）
- Round 2-6：缓存命中，响应时间 0ms（instant）
- 缓存机制有效：L0 cache key=query原文，Search cache key=query原文，L4 cache key=md5(query+context)
- 上次 Round 2/3 没命中是因为测试代码里根本没有缓存实现，每次都是全新推理
- 加上缓存层后，相同 query 第2次起就是 0ms 响应

## 18:04 - 连续3次深度搜索性能对比测试（qwen3.6 vs qwen3.5:4b）
测试问题："五一杭州有哪些玩的"

【qwen3.6:latest 思考模型 3轮测试】
- L0路由：平均 609ms（稳定，smollm2_ollama via）
- L4耗时：平均 33670ms（34s），不稳定
- 生成字数：Round1=353字（好）/ Round2=78字（草稿）/ Round3=34字（草稿）
- 问题：思考模型不稳定，内容在 thinking 字段，且最后段落可能是思维结构而非答案

【qwen3.5:4b 思考模型 3轮测试】
- L0路由：平均 620ms（稳定）
- L4耗时：平均 25913ms（26s），更稳定
- 生成字数：Round1=576字 / Round2=1043字 / Round3=1003字
- 内容质量明显优于 qwen3.6:latest

【关键踩坑】
- 流式调用（stream=True）对于所有 Ollama 思考模型，content 字段仍为空
  所有思考模型均通过 message.thinking 字段返回内容
- /no_think 指令对 qwen3.6:latest 无效，模型仍进入思考模式
- 推荐 L4 增强阶段使用 qwen3.5:4b 而非 qwen3.6:latest（非思考任务场景）

## 18:16 - 统一缓存封装 unified_cache.py + 集成测试验证通过
- 编写 unified_cache.py，整合系统现有12种缓存实现，为深度搜索链路提供统一接入点
- 核心架构：
  * L0RouteCache: 自实现（100条/24h/LRU），复用 smolllm2/router.py 的设计模式
  * SearchCache: 封装 tier_model/cache_manager.py 的 CacheManager（L1内存/L2 SQLite/L3 FAISS）
  * L4ResponseCache: 封装 fusion_rag/exact_cache.py (ExactCacheLayer, LRU+SimHash, 7天TTL) + fusion_rag/write_back_cache.py (异步回填)
- 全局单例：get_unified_cache() 提供线程安全的统一入口
- 集成测试 test_deep_search_cached.py 结果：
  * Round1 冷启动：L0 361ms + L4 29.9s = 总计 29.9s
  * Round2/3 缓存命中：L0 ✅ 0ms + Search ✅ L1 HIT + L4 ✅ 0ms = 总计 0ms
  * 综合命中率 62.5%，总计节省 21.7s 延迟
- 已知问题：
  * WriteBackCache 异步回填在同步上下文触发 RuntimeWarning（无害，已加 try/except 降级）
  * qwen3.5:4b 生成内容含思考片段（"Wait, one more thing"），需优化提示词模板

## 17:13 - 深度搜索任务链解析与重新执行
- 解析深度搜索完整任务链：Agent入口 → Wiki系统 → Wiki生成器 → 搜索引擎 → 数据模型
- 执行3路并行搜索：景点(10条) + 美食(10条) + 行程(10条) = 30条高质量结果
- 结果质量评估：信息时效性⭐⭐⭐⭐⭐ (6条2026最新攻略)、来源权威性⭐⭐⭐⭐、内容覆盖度⭐⭐⭐⭐⭐
- 输出结构化攻略：景点/美食/行程/实用提示四大维度，含预约提示、避坑指南、预算参考
